name: 定时抓取

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: run start.sh
        run: |
          pwd
          cd zhihu_hotlist
          bash ./start.sh
      - name: check
        run:
          git log  
